
# Used by lazy.nvim config to find where plugins in dev-mode are located:
export LAZY_NVIM_DEV_PATH="$HOME/.dotfiles/nvim/lua/dev"

# Point neovim to a nodenv-managed node:
export NVIM_NODE_HOST_PROG_PATH="$HOME/.nodenv/versions/20.2.0/bin"

